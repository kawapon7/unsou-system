# リモート運用（mini / Air / iPhone）は別リポジトリへ

HIBIKI の開発内容と無関係なため、2026-09-05 に `~/dev/blackice_note`（GitHub: kawapon7/blackice_note・private）へ移した。

- 正本: `~/dev/blackice_note/docs/REMOTE_OPS.md`（困ったらまずここ）
- 健康診断: `ssh mini ~/dev/blackice_note/ops/doctor.sh`
- 常駐の係（`claude remote-control`）は今まで通りこのリポジトリの中で動く

このリポジトリに残っている関連文書は `docs/REMOTE_DEV_CHECK.md`（実データで dev サーバーを見る手順・HIBIKI 固有）だけ。
