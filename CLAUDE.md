# CLAUDE.md

## 開発環境・基本構造
- **技術スタック**: Next.js (App Router), Supabase (Auth/DB), Cloudflare Workers (@opennextjs/cloudflare), Resend
- **基本ルール**: 画面遷移、ルーティング、テーブル定義などの個別仕様は、常にプロジェクトルートにある最新の仕様書（`HANDOVER_MASTER.md`等）を直接読み込んで完全同期すること。本ファイルには仕様変更の影響を受けない「共通の行動規範」のみを定義する。

## クラス最高の自律実装ルール

### 1. 出力形式（結論ファースト・手順は省略しない）
優先順位はグローバル `~/.claude/CLAUDE.md` に従う: **安全 > 無駄の回避 > 最適性**。

- **手順は踏む。報告は簡潔にする。** この2つは別物として扱う。プロセスを省略しないことと、経過を長々と出力することを混同しない。
- 挨拶・過剰な謝罪・定型の前置きは省く。ただし**判断根拠は隠さない** — 結論と、その根拠を1〜3行で示す。詳細は求められたときに出す。
- **スキルを使う場合は「Using [skill] to [purpose]」の宣言を行う。** チェックリスト・TODOは可視化する（進捗管理として機能するため）。どのスキルを使うかは状況に応じて最適に選ぶ（§4参照）。
- brainstorming、systematic-debugging、writing-plans などプロセス系スキルが適用可能な場合、そのプロセス（思考・計画・確認手順）は**省略せず実行する**。ただし出力するのは各段階の結論であり、思考の全過程ではない。
- コード変更は diff 中心で示す。ファイル全体の再掲はしない。

### 2. セキュリティ・防衛的設計（インシデント防止）
- **本番環境への変更は一括で進めない**: 本番DB・本番デプロイに影響する操作は1コマンドずつ提示し、都度確認を取ること。
- **ビルド成果物のコミット厳禁**: `.next/` および `.open-next/` などの自動生成ディレクトリを絶対にステージングに巻き込まないこと。`git add` 時は対象ファイルを明示するか、実行後に必ず `git status` で目視確認を徹底すること。
- **口座情報の暗号化**: 口座情報のDBへの平文保存は厳禁。必ずサーバーサイドで `utils/crypto.ts` の AES-256-GCM ユーティリティを使用すること。
- **RLSのバイパスと制限**: クライアント（ブラウザ）からのDB直接クエリは全面禁止。データアクセスはすべて Server Actions を経由させること。
- **不変ログの保護**: `approval_history` および `notification_logs` への `UPDATE` / `DELETE` は全ロールで実行禁止（INSERTのみ許可）。

### 3. コーディングと技術提案のルール
- **実動コードの提供**: 原則としてそのまま動作する検証済みコードを出力すること。環境依存等で検証不可の場合は「未検証」と明記すること。
- **エラー警告のコメント**: API制限、非同期処理の待機、データ型の不一致など、エラーが予想される箇所にはコード内に必ず注意喚起のコメントを入れること。

### 4. スキル活用の優先順位
- hibiki-dev / hibiki-security を含む各スキル（superpowers 群も対等）を、状況に応じて最適と思われるものを選んで使用し、その使用を明示すること。ただし安全に関わるもの（hibiki-security の発動トリガー該当時、および git コミット前3ステップ）は常に遵守し、この選択の対象外とする。
- 「最速で結果だけ出す」ことより「正しい手順を踏んで確実に動くものを作る」ことを優先する。ただしこれは「回り道をしてよい」という意味ではない（安全 > 無駄の回避 > 最適性）。

### 5. 画面検証とトークン消費（本プロジェクト固有）
本プロジェクトは Web アプリのため画面検証が頻発する。ここが最大のトークン消費源になる。

- **スクリーンショットは1枚あたり約35,000トークン**（画像は Base64 文字列として会話に積まれ、以降の全ターンで再送される）。実測で1セッション6枚 = 約217,000トークンを消費した事例がある。
- 画面の**内容**（文字・要素・構造）の確認は `read_page` を使う。桁違いに軽い。
- スクリーンショットは**レイアウト崩れ・見た目の確認が本当に必要な場面に限る**。撮る場合も1回で済むよう、確認したい状態を作ってから撮る。
- エラー確認は `read_console_messages` / `preview_logs` / `read_network_requests` を優先する。
- `execute_sql` や Bash の出力は、件数が多くなる場合 `LIMIT` / `tail` で絞ってから受け取る。
- タスクが1つ完了したら、状態を `HANDOVER_MASTER.md` 等に追記してから `/clear` を提案する。

### 6. モデルの使い分け（既定は Sonnet 5）
**既定は Sonnet 5。** 重い作業に該当すると判断した時点で、着手前に「これは Opus 5 推奨」と**自分から申告する**。
切り替え操作自体はユーザーがモデル選択UIから行う（アシスタント側からは切り替えられない）。

判断軸: **既にパターンがある作業は Sonnet、パターンを決める作業は Opus。**

**Opus 5 を推奨するケース**（該当したら申告する）
- マルチテナント分離（RLS）の実装など、全テーブル・全アクセス経路に横断し、1箇所の漏れが不可逆な被害になる変更
- セキュリティ監査、および `utils/crypto.ts`・`approval_history`・`notification_logs` に触れる変更
- 原因不明のバグ（同じ問題で5回試して直らないものは、粘らずモデルを上げる方が安く済む）
- 暗黙の罠が絡む変更（`'use server'` のバレ型再エクスポート等、書いた本人には見えない類）
- 本番DB・スキーマ・不可逆操作の設計判断

**Sonnet 5 で進めるケース**（申告不要、黙って進める）
- 画面追加・修正、CRUD、バリデーション追加
- 既存の型に沿った複製系実装（Server Actions + RLS の確立済みパターン）
- 単純なマイグレーション、デモデータスクリプト、文言・UI調整
- `HANDOVER_MASTER.md` / 設計書の更新
- ログを読んで原因箇所を特定するまでの一次切り分け

**Sonnet 5 で進める場合の対価: 検証を省略しない。**
「動きました」で終わらせず、画面（`read_page`）かログで実際に確認した結果を示す。
ここを省くとプラン代を検証工数と手戻りで払うことになる。

**迷う程度なら Opus 側に倒す**（安全 > 無駄の回避）。ただし「迷う」を口実に既定へ戻さない。

**サブエージェント（Agentツール）のモデル選択**
メインセッションと違い、Agentツールでサブタスクを切り出す際は `model` パラメータを**自分の判断で指定してよい**（ユーザー確認不要）。判断軸は上記と同じ「パターンがあるかどうか」。

- Opus: 設計判断・横断調査・複雑なロジック監査を伴うサブタスク
- Sonnet（既定）: 仕様が明確で、既存パターンに沿って計画通り実装するサブタスク
- Haiku: ブリーフに全コードが記載済みの写経・定型複製など、判断の余地がほぼないサブタスク

迷ったら指定せず、親と同じモデル（既定Sonnet）に流す。