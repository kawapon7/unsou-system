# Dependencies
node_modules/

# Environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# Next.js
web/.next/
.next/
web/out/
web/build/

# Build outputs (Cloudflare / OpenNext)
.open-next/
web/.open-next/
.wrangler/
web/.wrangler/

# Supabase local
supabase/.branches/
supabase/.temp/

# macOS
.DS_Store

# Debug
npm-debug.log*
yarn-debug.log*

# Git worktrees
.worktrees/

# Claude Code ローカル設定（端末ごとの個人設定。共有しない）
.claude/settings.local.json

# 実データ（口座情報・角印・従業員実名を含む。Git管理下に置かない）
# ⚠️ ルート直下限定。無印の "ooba/" だと web/src/components/pdf/formats/ooba/（ソースコード）まで
#    誤って無視してしまうため /ooba/ に絞る（2026-08-23 Task 6 で発覚）。
/ooba/
/oobaunsou_mihon/
